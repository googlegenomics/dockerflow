name: TaskTwo
inputParameters:
- name: inputFile
  type: file
- name: message
  defaultValue: goodbye
outputParameters:
- name: outputFile
  type: file
docker:
  imageName: ubuntu
  cmd: |
    mkdir -p $(dirname ${outputFile})
    cp ${inputFile} ${outputFile}
    echo ${message} >> ${outputFile}