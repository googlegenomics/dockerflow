#!/bin/sh
: "${DOCKERFLOW_HOME:?Error: DOCKERFLOW_HOME not set. It should be the dockerflow installation folder}"

java -cp ${DOCKERFLOW_HOME}/target/dockerflow*dependencies.jar com.google.cloud.genomics.dockerflow.Dsub "$@"
